# Vercel Deployment Information

This deployment uses only the **frontend** code. 

## Key Details

- **No Backend Code Used**: The backend code in this repository was not used for the Vercel deployment.
- **Frontend Only**: Only the `frontend` directory was used to deploy this application to Vercel.


## Rationale

To simplify the deployment process and leverage Vercel's optimized hosting for Next.js, the backend code has been bypassed or integrated as static/client-side logic where applicable. No external backend service is required for this specific deployment instance.
